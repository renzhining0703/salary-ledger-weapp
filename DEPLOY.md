# 薪账本 · 上线部署方案

> 从「本地可跑」到「线上可用」的完整步骤。总耗时约 **1~2 个工作日**（主要等审核）。
> 前置信息：AppID `wx7326e353b2996845` ｜ 云环境 `cloud1-8gembxhfa18dcf14` ｜ 个人主体小程序

---

## 0. 现状盘点（哪些已就绪）

| 项 | 状态 | 说明 |
|---|---|---|
| 小程序账号 | ✅ | 已注册，个人主体（免费） |
| 云开发环境 | ✅ | `cloud1-8gembxhfa18dcf14` |
| 代码工程 | ✅ | 4 Tab 页面 + 骨架屏 + 动画全部完成 |
| 订阅消息模板 ID | ✅ | 前端 `config.js` 与云函数 `remind/index.js` 已同步（本次修复） |
| 5 个数据库集合 | ⚠️ 需确认 | `users / salary / cards / expenses / recurring`，权限设「仅创建者可读写」 |
| 2 个云函数部署 | ⚠️ 需确认 | `login`、`remind` 需右键「上传并部署：云端安装依赖」 |
| 隐私保护指引 | ❌ 未做 | 上线前必填（见 2.3） |

---

## 1. 上线前代码准备（改 1 个开关，核对 1 处模板）

### 1.1 关闭示例数据（必做）

新用户上线后不该看到演示数据。已加开关，**发布前改一处即可**：

```
utils/config.js
  DEMO_DATA: true   →   DEMO_DATA: false
```

> 改为 false 后，新用户首次进入只建用户配置，从零开始记账；你自己账号的数据不受影响。

### 1.2 核对订阅消息模板字段（必做）

模板 ID 已同步，但推送的字段 key（`thing1 / date2 / amount3`）必须与你申请的模板关键词**类型一致**，否则推送报「模板关键词不匹配」。

1. 登录 [mp.weixin.qq.com](https://mp.weixin.qq.com) → 功能 → 订阅消息 → 查看你选的模板
2. 对照模板里的关键词顺序，核对 `cloudfunctions/remind/index.js` 中 `data` 的字段：
   - `thing1` → 模板第 1 个关键词须为「事物」（如：还款名称）
   - `date2` → 模板第 2 个关键词须为「日期」（如：还款日期）
   - `amount3` → 模板第 3 个关键词须为「金额」（如：还款金额）
3. 若模板字段类型/顺序不同，把 `thing1/date2/amount3` 换成模板实际的 key（如 `thing4`），并保持 value 格式一致

> 个人主体可选模板有限，找不到「还款提醒」类时，可选用「记账提醒/待办提醒」等通用模板，把推送文案调整成对应含义即可。

### 1.3 体验版联调推送的版本开关（可选）

云函数 `remind/index.js` 中 `miniprogramState: 'formal'`：
- **体验版测试推送** → 临时改为 `'trial'`，重新部署
- **正式发布后** → 保持 `'formal'`（当前值已正确）

---

## 2. 后台配置（一次性，约 30 分钟）

### 2.1 数据库集合与权限

云开发控制台 → 数据库 → 确认 5 个集合存在：

| 集合 | 用途 | 权限 |
|---|---|---|
| `users` | 用户配置（发薪日/预算/昵称头像） | **仅创建者可读写** |
| `salary` | 工资记录 | **仅创建者可读写** |
| `cards` | 信用卡记录 | **仅创建者可读写** |
| `expenses` | 日常开销记录 | **仅创建者可读写** |
| `recurring` | 每月固定支出模板（手动确认记账） | **仅创建者可读写** |

> ⚠️ 新建集合默认是「所有用户不可读写」，读写会全失败，务必逐个改成「仅创建者可读写」。
> `recurring` 未创建时记账页也能正常打开（按空列表兜底），但无法使用固定支出功能，上线前务必建好。

### 2.2 部署云函数

开发者工具左侧资源管理器：
1. 右键 `cloudfunctions/login` → **上传并部署：云端安装依赖**
2. 右键 `cloudfunctions/remind` → **上传并部署：云端安装依赖**
3. 右键 `cloudfunctions/finReport` → **上传并部署：云端安装依赖**（本月账单 AI 解读，可选）

部署后提醒功能自动生效（定时触发器每天 09:00 执行，见 `config.json`）。

### 2.3 配置 AI 解读（可选）

`finReport` 云函数默认关闭，需要时按以下步骤开启：

1. **申请 DeepSeek API key**：https://platform.deepseek.com → 注册 → API keys → 创建。充 1 元够用一年。
2. **配置环境变量**：云开发控制台 → 云函数 → `finReport` → 配置 → 环境变量，添加：
   - `LLM_API_KEY` = `sk-xxx`（你的 key，**不要写进代码**）
   - `LLM_BASE_URL` = `https://api.deepseek.com`（可选，默认就是这个）
   - `LLM_MODEL` = `deepseek-chat`（可选）
3. **创建数据库集合**：云开发控制台 → 数据库 → 添加集合 → 输入 `finReports` → 权限「仅创建者可读写」（用于缓存每月解读）。
4. **重新部署** `finReport` 云函数一次（让环境变量生效）。

不开也能用：用户在「记账」Tab 点月度总览卡，会先用本地模板生成一段朴素的财务小结；8 秒内 AI 没回就保留模板版。

#### 成本与安全

- 每次调用约 500 字输入 + 200 字输出，DeepSeek 单次约 0.0003 元，每月一次几乎免费。
- API key 只放云函数环境变量，代码里写 `process.env.LLM_API_KEY`。
- **定期 rotate**：若 key 不慎泄露（聊天记录 / 截图 / 公开仓库），去 DeepSeek 控制台「删除」该 key 再建一个新的，然后在云函数环境变量里换掉，重新部署即可。

### 2.4 隐私保护指引（审核必查）

小程序后台 → 设置 → **用户隐私保护指引** → 声明：

- 收集信息：**头像、昵称**
- 用途：用于多用户场景下展示身份（自愿填写，可跳过）
- 其他项按页面指引勾选「不涉及」

### 2.4 服务类目

小程序后台 → 设置 → 基本设置 → **服务类目**：

- 建议选：**工具 → 记账**（个人主体可选）
- 若审核提示类目不符，可改「工具 → 效率」等宽泛类目

---

## 3. 本地自检（提交前必过）

### 3.1 功能走查清单

| # | 检查项 | 预期 |
|---|---|---|
| 1 | 首次进入 | 自动登录，首页出现空态/示例数据（按 DEMO_DATA 开关） |
| 2 | 记录发薪 / 添加信用卡 / 记一笔 | 弹层滑入滑出动画正常，保存后列表立即出现 |
| 3 | 首页「今天要处理」 | 明天还款黄标、当天红标、逾期红标，标记已还后消失 |
| 4 | 月度结余看板 | 左右切换月份数字正确，结余正绿负红 |
| 5 | 预算预警 | 超支红条 / 80% 黄条按当月开销正确触发 |
| 6 | 设置弹层 | 改头像/昵称/发薪日/预算后保存生效 |
| 7 | 还款提醒授权 | 点「还款提醒」弹出订阅授权，同意后授权成功 |
| 8 | 数据隔离 | 用两个微信号分别登录，互看不到对方数据 |

### 3.2 定时提醒联调（建议）

1. 把 `remind/index.js` 的 `miniprogramState` 改为 `'trial'`，重新部署
2. 在云开发控制台 → 云函数 → remind → 测试 → 手动触发一次
3. 检查返回 `{ checked: n, due: m, sent: k }`，`sent ≥ 1` 表示推送成功
4. 手机微信收到「明天还款」通知即联调通过，改回 `'formal'` 重新部署

---

## 4. 上传体验版 → 提交审核 → 发布

### 4.1 上传体验版

| 步骤 | 操作 |
|---|---|
| 1 | 开发者工具右上角「**上传**」→ 版本号填 `1.0.0`，备注填本次改动 |
| 2 | 小程序后台 → 版本管理 → 开发版本 → 找到该版本 → 点「**设为体验版**」 |
| 3 | 手机微信扫码体验版二维码，完整跑一遍 3.1 走查清单 |
| 4 | 拉 1-2 个朋友扫码测试（个人主体最多可添加体验成员） |

### 4.2 提交审核

| 步骤 | 操作 |
|---|---|
| 1 | 后台 → 版本管理 → 开发版本 → 「**提交审核**」 |
| 2 | 填写审核备注：简述功能（个人记账工具，含还款提醒） |
| 3 | 按页面提示上传功能截图（首页/工资/信用卡/记账 4 张即可） |

审核常见驳回原因与对策：

| 驳回原因 | 对策 |
|---|---|
| 类目不符 | 改用「工具 → 记账/效率」类目（见 2.4） |
| 未声明隐私信息 | 补「用户隐私保护指引」（见 2.3） |
| 功能描述不清 | 审核备注写清楚：纯个人记账，无支付、无社交、无广告 |
| 无实际内容/占位 | 确认体验版数据可用、无「开发中」占位文案 |

> 个人主体小程序审核一般 1~7 天，通常 1-2 天出结果。

### 4.3 发布上线

| 步骤 | 操作 |
|---|---|
| 1 | 审核通过 → 后台 → 版本管理 → 「**发布**」→ 全量发布 |
| 2 | 发布后手机搜小程序名称或扫码，验证线上版可用 |
| 3 | 确认 `remind/index.js` 为 `miniprogramState: 'formal'` 且已重新部署 |

---

## 5. 上线后运营

| 事项 | 说明 |
|---|---|
| 云开发控制台 | 定期看云函数调用量/错误日志（免费额度个人用不完） |
| 订阅消息失效 | 微信规定一次授权只推一次；建议首页保留「还款提醒」按钮，用户每次打开顺手点一下续期 |
| 新用户引导 | 首次进入为空数据，可后续迭代加「一键导入/示例教程」 |
| 版本迭代 | 改代码 → 上传新版本号 → 审核 → 发布，流程同第 4 节 |

---

## 6. 部署时间线总览

| 阶段 | 内容 | 预计耗时 |
|---|---|---|
| 第 1 步 | 代码准备（开关 + 模板核对） | 10 分钟 |
| 第 2 步 | 后台配置（集合权限/云函数/隐私/类目） | 30 分钟 |
| 第 3 步 | 本地自检 + 提醒联调 | 30 分钟 |
| 第 4 步 | 上传体验版 + 朋友测试 | 0.5~1 天 |
| 第 5 步 | 提交审核 | 1~7 天（通常 1-2 天） |
| 第 6 步 | 发布 + 线上验证 | 10 分钟 |

### 补充：回收站与隐私锁（无需新建集合）

- 回收站采用**软删除**：删除记录只是在原文档上标记 `deleted: true + deletedAt`，保留 30 天后由小程序启动时自动物理清理，不引入新集合。
- 隐私锁配置存于 `users` 集合字段：`privacyLock`（off / gesture / finger）、`gesturePattern`（手势点序），同样无需建集合。
