# 账单页 AI 总结月初失真修复

## 问题

9月2日账单页「账本君总结」输出「收入为 0、固定支出占支出比例 2289%」：

- 支出仅 ¥179、固定支出 ¥4100 → `recurTotal/expense*100 ≈ 2289%`，小分母比率无意义却被硬塞给 LLM
- 数据块不含累计可用余额/历史结转，AI 不知道有上月结余可花，误把「收入为 0」当风险
- 月初语境缺失，AI 不知道环比大跌、收入未记属正常

## 修复内容

### pages/statement/statement.js
- `_buildStatement` 返回原始 `available` / `carriedOver` 数字（与首页看板同源）
- `_callFinReport` 补传 `available`、`carriedOver`、`today`（仅查看当前月时传 today，历史月不传避免误导）

### cloudfunctions/finReport/index.js
1. **数据块补上下文**（formatDataForLLM）：
   - `今天：2026-09-02（本月第 2 天，月初，当月数据还不完整）`
   - `累计（截至本月）：可用余额 ¥6200，含历史结转 ¥4379`
   - 月初环比自动标注「参考价值低」
2. **小分母护栏**：
   - 储蓄率：income = 0 时不输出（月初工资未发属正常）
   - 固定支出占比：仅 expense ≥ ¥500 且占比 ≤ 200% 时输出，否则降级为「全月应付口径，尚未全部发生，不算占比」
3. **SYSTEM_PROMPT**：
   - 新增「月初场景」规则：月初收入为 0 属正常、优先以累计可用余额评价余粮、环比大跌不必惊讶
   - 硬约束新增：禁止对不同口径数字（固定支出÷当月已支出）自行算占比
4. **缓存版本化**：`CACHE_VER = 2`，finReports 文档带 `ver` 字段，旧版本缓存自动视为未命中 → prompt 变更后无需手动清集合

## 验证

`scripts/verify-finreport-monthstart.js`：29 项断言全过，含失真场景实跑（抽取 formatDataForLLM 真实执行）：

```
本月：2026年9月
今天：2026-09-02（本月第 2 天，月初，当月数据还不完整）
收支：收入 ¥0，支出 ¥179，结余 -¥179
累计（截至本月）：可用余额 ¥6200，含历史结转 ¥4379
对比：上月支出 ¥4100（环比 ↓95.6%，月初环比参考价值低）
分类（降序）：餐饮 ¥179(100%,未设预算)
固定支出 ¥4100（全月应付口径，尚未全部发生，不算占比）
```

月中场景回归：占比/储蓄率/环比均正常保留，不误标月初。历史月不输出日期行。

## 部署清单

1. 微信开发者工具 → 云函数 finReport → 右键「上传并部署：云端安装依赖」
2. 小程序重新编译（statement.js 有改动）
3. 无需手动清 finReports 缓存（版本号自动失效）
