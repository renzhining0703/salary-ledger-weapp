# 账本君首页 chat 工资提醒复读与过早提醒修复

## 问题

用户截图反馈：

1. **每轮都复读**：无论问"今天花了多少"还是"上个月花了多少"，账本君都追加同一句："另外提醒一句，这个月好像还没记工资，等15号发薪记得记上。"
2. **提醒过早**：今天才 9 月 2 号，发薪日是 15 号，提前 13 天就开始催记工资。

## 根因

`cloudfunctions/finChat/index.js` 的 PROMPT_HEAD 中写死了无条件规则：

> 收入为 0 但支出 > 0 → 第一句先提醒"这个月好像还没记工资"，再基于支出/预算继续回答

只要本月还没记工资，LLM 就会在每轮回答里复读。

## 修复

只改 `cloudfunctions/finChat/index.js`：

1. **系统提示去 unconditional**：删除"收入为 0 但支出 > 0 就提醒"的硬规则，改为：
   - 只有【本月数据】里出现「工资提醒：...」这一行时，才允许顺带提一句
   - 没有该行时禁止主动提工资/发薪日
   - 同一会话里已经提醒过，禁止重复

2. **数据块里条件注入工资提醒**（`formatDataForLLM`）：
   - 触发窗口：**发薪日前 3 天 ～ 发薪日后 5 天**
   - 且当月收入为 0
   - 且同会话未提醒过
   - 且非 record 模式
   - 今天 9 月 2 号、发薪日 15 号 → 距发薪还有 13 天，**不提醒**

3. **同会话去重**（`buildMessages`）：
   - 扫描本轮对话历史，如果 assistant 已经说过"还没记工资"，设置 `data.salaryReminded = true`，`formatDataForLLM` 就不再输出工资提醒行。

4. **硬约束压轴**：在 `PROMPT_TAIL` 里再次钉死：工资提醒只能由数据块触发、禁止重复、禁止自行创造。

## 验证

`scripts/verify-salary-reminder.js`：19 项断言全过，实跑数据块如下：

**9月2日 / 发薪日15号 → 不再提醒**
```
本月：2026年9月
今天：2026-09-02（周三），本月还剩 29 天
发薪日：每月15号
收支：收入 ¥0，支出 ¥167，结余 -¥167，储蓄率 0%
累计可用余额（含历史结转）：¥6000
```

**9月13日 / 发薪日15号 → 提醒"还有2天"**
```
工资提醒：本月还没记工资，发薪日是每月15号（还有2天），到账后记得补上
```

**9月15日当天 → 提醒"今天发薪"**

**9月17日 → 提醒"已过期2天"**

**9月23日 → 不再提醒**

**已记工资 / 同会话已提醒过 / record 模式 → 均不提醒**

## 部署清单

1. 微信开发者工具 → 云函数 finChat → 右键「上传并部署：云端安装依赖」
2. 小程序重新编译（本次只改云函数，前端无需改动；但保险起见可重编译一次）
