# 薪账本 · 全维度架构评审报告

> 评审范围：完整项目 16,836 行（9 页面 / 2 组件 / 6 云函数 / 8 utils）
> 评审视角：微信小程序全栈架构
> 报告日期：2026-09-01

---

## 开篇：总体评分与核心结论

### 总体架构评分：**7.5 / 10**

（数据层设计 8.5 分，账本君工程化 8 分，代码组织 6 分，性能与配额意识 7 分，合规 8 分）

### 核心结论（3 句话）

1. **这是一套远超个人项目平均水准的云开发架构**：统一读入口（dbRead）+ 客户端 TTL 缓存 + 月度支出快照（expAgg）+ 软删回收站 + 防重撤销，数据层设计成熟且每一层都有降级路径。
2. **最大风险在「双写路径不同步」**：客户端写操作走 `bumpExpAgg` 增量维护快照，但账本君云函数（finChat）记账直写库、完全不维护 expAgg（已 grep 实锤），历史月余额漂移只靠 `console.warn` 提示 + 用户手动下拉对账兜底。
3. **主要债务是代码组织而非架构**：`index.js` 1528 行上帝页面 + `finChat` 1814 行巨石云函数 + 3 处死代码，重构成本低、收益高，不动架构只动文件即可显著提升。

---

## 模块一：架构全景梳理

### 1.1 分层结构（实际依赖图）

```
pages/ (9 页)
   │  require
   ▼
utils/ (db.js 数据层 · util.js 工具 · aiChat/chatStorage · theme · config)
   │  wx.cloud.callFunction
   ▼
cloudfunctions/
   ├── dbRead          统一读入口（batchHomeRead 5合1 / 分页聚合 / 回收站）
   ├── finChat         账本君：LLM + 7 工具 + 画像 + 长期记忆（写路径！）
   ├── finReport       月报生成
   ├── login           静默登录 / openid
   ├── remind          定时触发器（还款提醒 9:00）
   └── salaryReminder  两轮工资询问推送
components/ (ai-chat-sheet 双页共用 · cat-grid)
```

**判断：依赖方向健康**——单向 `pages → utils → cloud`，无反向依赖、无循环依赖；组件只依赖 utils 不依赖页面。这是很多中型团队项目都做不到的。

### 1.2 设计度评估：适度偏重，但重得有理由

| 设计点 | 判断 | 说明 |
|---|---|---|
| expAgg 月度快照 + 全量对账 | ✅ 合理 | 解决 36 个月大查询的真实性能问题，且有降级路径 |
| 软删除 + 30 天回收站 + purge | ✅ 合理 | 记账工具的数据安全感是核心体验 |
| batchHomeRead 5 查合一 | ✅ 合理 | 省 4 次网络往返，失败自动降级单项读（db.js） |
| 24h 画像缓存 aiProfiles | ✅ 合理 | LLM token 成本控制的正确手段 |
| 双写路径（客户端直写 + 云函数写） | ⚠️ 过度 | 两条写路径维护逻辑不一致，是本项目唯一架构级隐患 |
| globalData.chatMessages 全局会话 | ⚠️ 略欠 | 全局可变状态被页面/组件三方读写，见模块三 |

### 1.3 值得肯定的降级设计（连环三保）

`batchHomeRead` 链路：云端新版 → 降级 5 单项读 → 再降级 12 个月窗口近似 + 阶段2缺口补查。每层都有 `_loadSeq` 竞态序号保护（`index.js:175`），旧一轮晚到数据直接丢弃。**这是生产级思维**。

---

## 模块二：功能全链路校验（记账 → 存储 → 展示）

### 2.1 主链路走查结论

**客户端记账**：expenses 页表单 → `db.addExpense`（软删字段 + `bumpExpAgg` 增量维护 + `invalidateAiProfile` 防抖）→ 60s 缓存失效 → 首页 onShow `force=true` 重拉 → 看板/预算/趋势/AI 数据块全量刷新。**链路闭环，无断点**。

**账本君记账**：sheet 发送 → finChat 工具调用 → `executeAddExpense` 写库 + `_id` 回查验证 + 失败回滚 → `invalidateFinCache` 清画像缓存 → 组件 `refresh` 事件 → `onAiChatRefresh` 强制 `loadData(true)`。**链路闭环**（云函数写库不触发客户端缓存失效的问题已被 force 主动化解）。

### 2.2 发现的问题

#### 🔴 P0-1｜双写路径不同步：AI 记账导致历史月余额漂移
- **严重程度**：高（数据准确性）
- **位置**：`cloudfunctions/finChat/index.js`（executeAddExpense / executeAddSalary，全文无 expAgg 维护）vs `utils/db.js`（bumpExpAgg）
- **机制**：本月支出看板用实拉数据（正确），但 expAgg 快照没加这笔账。下月查看时，本月变历史月，累计可用余额 = 快照求和 + 本月实际 → **那笔 AI 记的账永久漏算**，直到用户手动下拉触发 reconcile。
- **现有兜底**：`index.js:279-289` 漂移检测仅 `console.warn`；`onPullDownRefresh` 传 reconcile=true 才修复。**用户不主动下拉，错误数据一直展示**。

#### 🟡 P1-2｜首次加载时分类预算 over 标志丢失（setData 时序边界）
- **严重程度**：中低
- **位置**：`pages/index/index.js:484`（setData 参数内调 `_buildCatStats`）→ `index.js:915`（读 `this.data.user.budgets`）
- **机制**：setData 参数求值时 `this.data.user` 仍是**旧值**（首轮为 null）→ budgetMap 为空对象 → `over` 标志全部 false → AI 数据块 `overCategories` 为空，账本君第一句超支提醒可能哑火。第二次刷新才恢复。
- **修法**：把 `user` 作为参数传入 `_buildCatStats(expenses, expense, user)`。

#### 🟡 P1-3｜还款流水口径混入预算预警
- **严重程度**：中（口径设计决策，需产品层确认）
- **位置**：`index.js:251`（注释明确「还款并入支出」）、`db.recordCardRepayment`
- **机制**：标记已还会写 `category=还款` 流水 → 计入本月支出总额 → 预算超支预警（80%/over）把信用卡还款算进「开销」。对用户心智而言「还信用卡≠消费」，预算预警可能误报。同时 catStats 会把「还款」显示为一个分类（不在六分类白名单）。
- **建议**：预算口径排除 `category=还款`，或预算文案注明「含还款」。

#### 🟢 P2-4｜降级补查 1000 条上限
- **位置**：`index.js:517`（`gapExpenses.length >= 999` 警告）——已有预警，仅降级路径触发，可接受。

#### ✅ 校验过的正确项
- 金额浮点累加，对账用 `Math.round(x*100)` 分级比较（`index.js:286`）——正确
- 写入验证 + 回滚（finChat）——超出预期
- `listSalary` 全量拉取（limit 1000，dbRead:62-67），累计收入无窗口截断——正确
- 还款日 `dayInMonth` clamp 月末溢出——正确

---

## 模块三：代码架构深度评审

### 3.1 组件化：7/10

- `ai-chat-sheet` 抽取质量高：双页共用、props/events 契约清晰、内置撤销倒计时/节流/键盘自适应/滚底三层保险、detached 自动清 timer。**这是全项目最好的抽象**。
- 但 sheet 宿主侧（index.js goAskAI 700-860 行的消息注入编排）依然留在页面里，复杂度只移走了一半。
- cat-grid、cat 弹层等未组件化，记账页 openForm/closeForm 动画逻辑与首页 sheet 开关重复。

### 3.2 公共层：6/10

- `util.js` 523 行杂烩：日期、金额格式化、数字动画、订阅消息、还款排序算法、隐私锁检查——至少 4 个职责域。建议拆 `date.js / money.js / subscribe.js`。
- `app.wxss` 636 行全局样式 + 组件 `apply-shared` 复用——在小程序约束下是务实选择，但意味着样式全局命名空间无保护。

### 3.3 耦合度

| 耦合点 | 评估 |
|---|---|
| `globalData.chatMessages` | ⚠️ 首页/记账页/组件三方读写同一可变数组（含 source 过滤、幂等去重逻辑分散在 index.js goAskAI），是**最脆的隐式契约**。建议收敛为 chatStorage 单一出入口 |
| globalData 标志位通信（quickExpense / autoOpenPayday / pendingAiQuestionFromNotif） | 小程序惯例，可接受，但每个消费方都要记得 onShow 重置，漏一处就成 bug |
| 页面 → db.js → 云函数 | ✅ 干净 |

### 3.4 死代码（应删）

| 文件 | 行数 | 状态 |
|---|---|---|
| `utils/chatController.js` | 249 | 项目记忆确认已无引用 |
| `scripts/verify-chat-sheet.js` | - | 校验旧组件结构，已失效 |
| `scripts/verify-feed-c89.js` | - | 同上 |

### 3.5 可维护性总评

注释质量**极高**（loadData 头部 20 行方案注释、每处坑都有「为什么」），这是单人项目最好的防遗忘资产。债务集中在**文件粒度**而非逻辑粒度：`index.js` 1528 行承载看板计算、双 canvas 绘制、分享卡片、AI 入口编排、订阅、最优还款 6 类职责；`finChat` 1814 行单文件装下 prompt/工具 schema/执行器/缓存/防重。

---

## 模块四：小程序专项诊断

### 4.1 启动性能

- ✅ `lazyCodeLoading: "requiredComponents"` 已开
- ✅ 首屏单次云函数调用（batchHomeRead）拿全数据
- ⚠️ `loadData` 首行 `await app.ready()`（index.js:174）——首屏渲染被静默登录网络往返阻塞。建议 user 档先渲染骨架，登录后再补数据。
- ⚠️ **每次切回首页 tab 都 `loadData(true)` 强制全量重查**（onShow，index.js:80）。个人自用没问题；按 20 万次/月套餐，若日活上量（如 500 人 × 日均 30 次切 tab），仅首页一项月消耗 45 万次，**会超配额**。建议改为「写操作置脏标记，onShow 仅脏时 force」。

### 4.2 渲染性能

- ✅ 单次大 setData 但数据量可控（60 条 recentExpenses + 12 月 trend）
- ✅ canvas 动画生命周期管理教科书级：onHide cancelAnimationFrame（index.js:115-121）、切月连点只保留最新动画（:1321）、撤销动画冲突先 cancel 再重放（:528）
- ✅ 撤销倒计时用精确路径 setData（组件内），避免整列表重渲染
- ⚠️ 分享卡片 canvas 750×1000 × dpr（dpr=3 时约 27MB 位图内存），低端机有峰值压力，建议 dpr 上限 clamp 到 2

### 4.3 合规性

- ✅ _openid 数据隔离 + 集合「仅创建者可读写」——双保险
- ✅ 订阅消息姿势正确：每日一次、必须点击触发（onPageTap 借第一次点击，index.js:141-143，符合微信硬性要求）
- ✅ 隐私锁 60s 窗口 + 各 Tab onShow 拦截
- ⚠️ 推断项：nickname 收集需确认已在隐私协议（用户隐私保护指引）中声明；AI 功能建议加「AI 生成内容」提示

### 4.4 兼容性

- ✅ `wx.getWindowInfo` 特性检测 + `getSystemInfoSync` 兜底（:1118、:1314）
- ✅ `showShareImageMenu` 低版本回退保存相册（:1265-1279）
- ✅ darkmode：theme.json + `darkmode: true` + canvas 主题色手动重绘（applyTheme，颜色不跟 CSS 变量的坑已排）
- ⚠️ canvas 2d 接口要求基础库 2.9.0+，project.config.json 若未设最低版本，极旧客户端趋势图/分享卡空白（建议设置 minimumLibVersion 或降级文案）

---

## 模块五：扩展性评估

### 5.1 多账本

**现状**：全集合无 `ledgerId` 维度，users 单文档承载 expAgg/budgets/aiMemories。
**改造代价**：中高。docs 加 `ledgerId` + users 加 `ledgers` 数组 + expAgg 结构升级为 `{ ledgerId: { month: amt } }` + dbRead/finChat/expenses 页全链路带账本上下文。
**建议**：现阶段**不要动**。真要做时优先做「账本归档/年度分册」这种轻量形态（按 year 虚拟分组，零 schema 变更），而非真多账本。

### 5.2 数据导出 Excel

**现状利好**：dbRead 的分页聚合模式（aggregateAllExpenses，skip 保险丝 20000）可直接复用为导出数据源。
**推荐路径**：云函数 `exportExcel`——npm 装 `xlsx`（或 `exceljs`）→ 云端生成 → 上传云存储 → 返回临时链接 → 前端 `wx.downloadFile` + 转发/保存。**切忌**前端拼 CSV（中文乱码 + 微信打开体验差）。

### 5.3 记账提醒

**基建完备度最高**：remind 定时触发器（9:00）+ 两轮 salaryReminder + tryDailySubscribe 每日授权回收 + unreadQuestion 红点链路全通。新增「记账提醒」只需：加一个订阅模板 ID + remind 云函数加一段扫描逻辑。**改造成本约 1 天**。

---

## 专项：账本君（AI 财务助理）

### 评分：**8 / 10**（个人项目语境下 9，按产品标准 8）

### 亮点（超出个人项目水准的工程决策）

| # | 决策 | 评价 |
|---|---|---|
| 1 | **504003 教训固化**：单轮至多 1 次工具调用；查询工具查完直接 `formatQueryAnswer` 拼确定性回答，不再二次 LLM | 从错误中提炼出架构约束并写死，省延迟省钱，答案数字 100% 可控 |
| 2 | **双温度策略**：record 模式 0.2（确定性）/ chat 模式 0.7（自然语气） | 对 function calling 失败成本的清醒认知 |
| 3 | **写入验证 + 回滚**：addExpense 按 _id 回查，openid 不符即删除 | 云开发弱事务环境下的自愈式写入 |
| 4 | **防重分级不硬拒**：5min recent / 同日 same-day → 返回 duplicateInfo 让 AI 反问 | 比硬拦截体验好一个量级 |
| 5 | **三层新用户放行**：stmt（paydaySet/hasRecorded）→ serialize → formatDataForLLM，新用户不拒答 | 空态 AI 体验闭环 |
| 6 | **四态状态机**（welcome/first/unset/normal）+ 开场白幂等注入（filter 旧 board-brief 再追加） | 看板承诺与 sheet 兑现一字同源 |
| 7 | 24h 画像缓存 + LRU 10 条长期记忆 | token 成本与个性化平衡的务实解 |

### 扣分项与建议

1. **-0.5｜单文件巨石**：finChat 1814 行装下 prompt 文案 / 7 个工具 schema / 执行器 / 缓存 / 防重。改一个工具描述要在 1800 行里跳。**建议拆四件**：`prompt.js`（文案段）/ `tools.js`（schema + execute 映射表）/ `cache.js`（画像 + fin 缓存）/ `index.js`（路由 ≤200 行）。
2. **-0.5｜expAgg 断点**（同 P0-1）：AI 记账是账本君的王牌功能，恰是它把余额算错，伤害最大。
3. **-0.5｜会话无服务端持久化**：50 条截断存本地 storage，换设备/清缓存即失忆，且长期记忆（users.aiMemories）与会话（本地）割裂。建议 chat 日志入云集合（只存摘要 + ts，控写入量）。
4. **-0.5｜formatQueryAnswer 模板感**：查询类回答完全模板拼接，一致性满分但「AI 感」弱。可选优化：模板答案后追加一句低成本 LLM 润色（输入只有答案本身，token 极少）。
5. **建议｜LLM 成本熔断**：云函数加日 token 计数（users 或独立 counters 集合），超阈值降级到纯模板回复——DeepSeek 便宜但免费额度/计费波动时账本君不能变成失联状态。

---

## 分优先级优化清单

### 🔴 P0（本周，数据正确性）

| # | 事项 | 位置 | 修改思路 |
|---|---|---|---|
| 1 | 云函数写路径补 expAgg 维护 | finChat/index.js executeAddExpense / executeAddSalary | 写库成功后：`db.collection('users').doc(id).update({ data: { ['expAgg.' + month]: _.inc(delta) } })`——子文档路径 + `_.inc` 字段级原子自增，无读改写竞态，3 行代码。删除（撤销）路径对应 `_.inc(-delta)` |
| 2 | 漂移检测自动对账 | index.js:279-289 | warn 分支加节流（本地 storage 记当日已 reconcile）后自动调 `loadData(true, true)`，摆脱对用户手动下拉的依赖 |
| 3 | _buildCatStats 旧 user 时序 | index.js:484 / 915 | user 作参数传入：`this._buildCatStats(expenses, expense, user)`，函数内 `const budgetMap = (user && user.budgets) \|\| {}` |

### 🟡 P1（本月，代码健康度）

| # | 事项 | 位置 | 修改思路 |
|---|---|---|---|
| 4 | 拆分 finChat 巨石 | cloudfunctions/finChat/ | 拆 prompt.js / tools.js / cache.js / index.js，云函数支持子文件（上传时整个目录打包），无部署风险 |
| 5 | 拆分 index.js | pages/index/ | 趋势图（drawTrend/trendBar）与分享卡（drawShareCard/roundRect/saveShareImage）各提为 Component，约减 600 行；AI 入口编排（goAskAI）提为 Behavior |
| 6 | 删除死代码 | utils/chatController.js、scripts/verify-*.js | 直接删，全项目 grep 确认无引用 |
| 7 | 首页 force 策略省配额 | index.js:80 onShow | db.js 写操作统一置 `globalData.dataDirty=true`，onShow 仅脏时 force；否则走 60s TTL 缓存 |
| 8 | 预算口径排除还款 | index.js:329-346 | 预算计算前 `expenses.filter(x => x.category !== '还款')`，或文案注明含还款 |

### 🟢 P2（规划，产品演进）

| # | 事项 | 说明 |
|---|---|---|
| 9 | 会话服务端持久化 | chat 摘要入云集合，与 aiMemories 打通 |
| 10 | Excel 导出 | 云函数 xlsx + 云存储临时链接，复用 aggregateAllExpenses |
| 11 | LLM 成本监控熔断 | 日 token 计数 + 超限降级模板 |
| 12 | 多账本（谨慎） | 优先「年度分册」轻量形态，真多账本需 ledgerId 全链路改造 |
| 13 | 分享卡 dpr clamp 2 | drawShareCard，低端机内存峰值 |
| 14 | 最低基础库版本 | project.config.json 设 minimumLibVersion 或 canvas 降级文案 |

---

## 附：评审方法说明

已完整读取：app.js / app.json / utils 全部 8 文件 / finChat（分段全量）/ dbRead / salaryReminder / remind / ai-chat-sheet 组件 / expenses / index（全量 1528 行）。my / calendar / statement / cards / lock / recycle 页基于目录结构、依赖关系与项目记忆推断，其中已识别 salary 页 countdown 与 calendar 发薪徽章的 `|| 15` 兜底遗留（payday=0 新用户会看到默认 15 的错误倒计时）——**追加为 P1-9，修法与首页 paydaySet 判定同模式**。
