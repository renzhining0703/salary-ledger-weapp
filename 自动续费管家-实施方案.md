# 自动续费管家 + AI 断舍离 —— 阶段 1/2 实施方案

> 文档性质：**给接手实现的前端 AI 的实现规格书**（不是给人类的产品 PPT）
> 前置阅读：`README.md`、`薪账本架构评审报告.md`
> 范围：**阶段 1（叠加验证）+ 阶段 2（AI 断舍离核心）**。阶段 3（变现/独立）另出文档
> 方向：**路线 C** —— 先叠加在薪账本上验证核心价值，跑通数据后再决定是否独立成「续费管家」

---

## 0. 一句话目标

在**不推倒现有架构**的前提下，把「订阅/自动续费」做成薪账本的一个新数据维度，复用账本君的 AI 能力，让用户能：**录入订阅 → 到期提醒 → AI 评估去留 → 出年度浪费报告**。阶段 1 只做「能记录 + 能提醒」，阶段 2 做「AI 断舍离 + 浪费报告 + 取消指引」。

---

## 1. 背景：为什么要做这个

### 1.1 用户痛点（真实的、普遍的）

几乎每个智能手机用户都踩过这些坑：

- 视频会员、网盘、音乐 App、各种「首月 0.1 元」的订阅自动续费，**忘了取消就被扣钱**
- 想整理时**算不清一年花了多少**，更分不清哪些是根本没用的「智商税」
- 想取消时**找不到入口**（iOS 要去 App Store 订阅管理，安卓要去支付宝/微信自动扣费，路径各不相同）

### 1.2 市场现状（机会判断）

- 有零星的「纯记录」工具，但 **带 AI 价值评估 + 取消指引 + 浪费分析的几乎没有**
- 大厂看不上（单用户价值低），但用户基数极大 —— 「人人都有需求但没人好好做」的蓝海
- 竞品（Bobby、订阅管家类 App）都在独立 App 形态，**小程序形态 + AI 断舍离是差异化**

### 1.3 为什么是现在（政策窗口）

2026 年 8 月底，微信刚开放 **个人主体小程序的「虚拟支付」能力**（这是做会员变现的前提，此前个人主体开不了微信支付）：

| 维度 | 现行规则 |
|---|---|
| 主体 | 个人 + 身份证，服务类目含「工具」 |
| 前置 | 完成小程序**认证 + 备案** |
| 额度 | 个人主体月收款限额 **10 万元** |
| 费率 | 安卓 **1%**，iOS 走苹果 IAP **12%** |

这是本方案「会员变现」能落地的**准入红利窗口**，越早进场越有利。

### 1.4 为什么复用薪账本而不是另起炉灶

薪账本最值钱的不是「记账」这个壳，而是这套 **AI 财务助手工程骨架**：

- `finChat`：function calling + 画像 + 长期记忆 + 成本熔断 + 限流
- `finReport`：数据聚合 + LLM 生成报告
- `remind`：定时触发器（每天 9:00）+ 订阅消息推送链路
- `db.js`：TTL 缓存 + 软删回收站 + expAgg 快照 + 写回滚
- `recurring`（固定支出）：**已经是「订阅」的雏形** —— 周期性扣款 + 金额 + 自动落账

订阅 = 固定支出的一个变体（多了平台/周期/取消指引/使用频率）。复用度约 **80%**，另起炉灶等于把最贵的 AI 基建重新做一遍。

---

## 2. 产品定位与命名决策

### 2.1 账本君是否更名

**结论：阶段 1/2 不改名。** 理由：

1. 「账本君」是薪账本 AI 财务助手的总人设，订阅评估只是它**新增的一个能力维度**，不是新人格
2. 更名会牵动 `finChat` 全部 prompt（"你是账本君"）、`finTemplate` 兜底文案、所有 UI 文案、订阅消息模板文案 —— 纯成本、无收益
3. 用户对「账本君」已有认知，中途改名反而割裂

**阶段 3 若独立成「续费管家」产品**，届时可选给订阅场景配一个副人格名，推荐候选：

| 候选 | 立意 | 我的倾向 |
|---|---|---|
| 断舍离君 | 延续「君」字辈，人设一致，直接点题 | **首选** |
| 订阅军师 | 突出「帮你拿主意」的参谋感 | 次选 |
| 续费管家 | 与产品名统一，但抢了产品名的位 | 不推荐 |

**核心原则：产品名与人设名分离。** 产品叫「续费管家」，AI 人设叫「账本君 / 断舍离君」，互不冲突。

### 2.2 小程序名推荐（你后续会变更）

**叠加期（阶段 1/2）**：仍叫「薪账本」，不动名字，定位语改为「记账 · 订阅续费 · 断舍离」。验证期改名是负资产。

**独立期（阶段 3 决定独立后）**：候选名（订阅管家方向）：

| 候选名 | 立意 | 评价 |
|---|---|---|
| **续费管家** | 直接点题、搜索友好 | 首选，但「管家」类名可能撞车，需查注册 |
| 扣费雷达 | 痛点（莫名被扣）+ 监控感 | 新颖易记，差异化强 |
| 订阅体检 | 点题「AI 评估体检」差异化 | 清爽，但「订阅」通用词 |
| 断舍离订阅 | 突出 AI 差异化 | 略长 |

**推荐：独立期首选「续费管家」，撞车则用「扣费雷达」。**

### 2.3 产品边界（Non-goals，明确不做什么）

阶段 1/2 **明确不做**：

- ❌ 截图 OCR 识别（放阶段 3；届时用 DeepSeek 视觉模型 deepseek-v4-flash-vision-exp，见「LLM 选型」小节）
- ❌ 自动抓取微信/支付宝真实扣费流水（API 不开放给第三方）
- ❌ 支付、广告、会员闸口（属阶段 3）
- ❌ 「使用频率」自动监测 —— AI 无法获知用户真实使用频率，只能靠**用户自评三档**（常用/偶尔/很少）
- ❌ 长期订阅消息（工具类拿不到，只能用一次性订阅）

---

## 3. 架构复用总览

| 现有能力 | 代码位置 | 订阅管家怎么复用 |
|---|---|---|
| 固定支出 recurring | `utils/db.js`（addRecurring/listRecurring/updateRecurring/removeRecurring/recordRecurring） | 订阅 CRUD 照抄这套，字段扩展 |
| 固定支出 UI 弹框 | `pages/my/my.js` + `my.wxml` + `my.wxss` | 订阅页 UI 参考此模板（recurList/recurScrollHeight 那套） |
| 统一读入口 | `cloudfunctions/dbRead/index.js` | 加 `listSubscriptions` action |
| 定时提醒 | `cloudfunctions/remind/index.js`（每天 9:00 触发器） | 加订阅到期扫描分支 |
| 账本君 function calling | `cloudfunctions/finChat/index.js`（TOOL_DEFS + execute 器） | 加 `addSubscription` / `evaluate_subscription` 工具 |
| 报告生成 | `cloudfunctions/finReport/index.js` | 仿写 `subReport`（年度订阅浪费报告） |
| 前端 AI 调用 | `utils/aiChat.js` + `utils/finTemplate.js` | 复用 send/serialize，加订阅摘要字段 |
| 订阅消息链路 | `utils/config.js` + remind | 加一个新模板 ID，复用授权回收 |
| 首页「今天要处理」待办区块 | `pages/index/index.js` + `index.wxml` | 升级为「待办账务」，合并还款 + 订阅两类待办（主入口） |

**依赖方向保持单向**：`pages → utils → cloudfunctions`，不引入反向依赖。

---

## 4. 数据模型设计

### 4.1 新增集合：`subscriptions`

云开发控制台手动创建，权限 **「仅创建者可读写」**（与现有集合一致）。

```js
// 订阅文档字段（字段名统一 camelCase，与 recurring 保持一致）
{
  _openid: 'xxx',            // 小程序端 SDK 自动注入；云函数端必须显式写（同 finChat 教训）
  name: '爱奇艺',             // 订阅名称（必填）
  platform: '爱奇艺',         // 平台名，用于取消指引匹配（AI 补全/用户手填，必填）
  payChannel: 'wechat',      // 扣费渠道：wechat 微信自动续费 / alipay 支付宝自动扣款 / apple App Store 订阅 / inapp 平台App内开通 / unknown 不清楚（决定取消指引主路径，见 T2.3 两级匹配）
  amount: 25,                // 单期金额（元，必填，>0，≤2 位小数）
  cycle: 'monthly',          // 周期：monthly / yearly / quarterly / weekly / custom（必填）。custom 用于非标准周期包（如腾讯视频「6个月88」），配合 customMonths 使用
  customMonths: 0,           // 仅 cycle=custom 时有效：一个周期含多少个月（如半年包=6、季包=3、两年包=24）。标准周期此字段为 0/忽略
  nextCharge: '2026-09-15',  // **下次扣费日期 YYYY-MM-DD（主录入字段）**——用户打开微信/支付宝/平台App照抄平台显示的「下次续费日」，零计算。录入后系统用 day(nextCharge) 反推 cycleDay
  cycleDay: 15,              // 扣费日，由 nextCharge 反推（monthly/quarterly/weekly 存「日」1-31；yearly 存「月-日」如 '09-15'；custom 时无 cycleDay）。每次扣费后由系统按周期推算更新 nextCharge
  firstChargeDate: '',       // 首次购买/扣费日期 YYYY-MM-DD（**可选**，用户记得就填，仅用于年度报告算「已订阅几个月」；不填系统用 nextCharge 减一个周期估算，不影响提醒主流程）
  status: 'active',          // active 使用中 / paused 已暂停 / cancelled 已取消
  usage: 'rare',             // 使用频率自评：frequent 常用 / occasional 偶尔 / rare 很少 / never 从不（默认 rare）
  lastUsed: '',              // 最近一次使用日期（可选，用户自填）
  cancelGuide: '',           // 取消指引文本（阶段 2 由内容库命中后回填，可为空）
  freeAlternative: '',       // 免费平替备注（阶段 2 AI 评估产出，可为空）
  note: '',                  // 备注（可选）
  demo: false,               // 演示数据标记
  deleted: false,            // 软删标记（进回收站，沿用现有 30 天清理机制）
  deletedAt: null,
  createdAt: Date,           // db.serverDate()
  updatedAt: Date
}
```

**关键约定**：

1. `nextCharge` 是**主录入字段 + 唯一到期判断依据**。录入时用户直接填「下次扣费日期」（照抄平台显示的续费日，零计算）；系统用 `cycleDay = day(nextCharge)` 反推导出扣费日。每次扣费到期后，系统按 cycle 滚动推算下一个 `nextCharge`（月末溢出用现有 `dayInMonth(y, m, day)` clamp，`remind/lib/date.js` 已有该函数；`cycle=custom` 时按 `customMonths` 整月累加）
2. `deleted: true` 走软删，纳入回收站 —— 需把 `subscriptions` 加入 `dbRead` 的 `RECYCLE_COLS` 数组
3. `usage` 是 AI 价值评估的**核心输入**，只有用户自评这一条路（见 Non-goals）
4. 年度年化金额口径：`monthly × 12`、`quarterly × 4`、`weekly × 52`、`yearly × 1`、`custom → amount × (12 / customMonths)`（如半年包 88 → 88 × 2 = 176/年；季包 45 → 45 × 4 = 180/年）

### 4.2 LLM 选型（全程 DeepSeek，不换厂商）

| 场景 | 模型 | 说明 |
|---|---|---|
| 阶段 1/2 文本链路（AI 录入 / 评估 / 年度报告） | `deepseek-chat`（V3.x） | 现有 `LLM_MODEL` 配置，**零改动** |
| 阶段 3 截图 OCR | `deepseek-v4-flash-vision-exp` | DeepSeek 视觉版，base_url 同 `api.deepseek.com`，按 Flash 价 |
| function calling 抽字段不稳时兜底 | 通义千问 `qwen-plus` | 仅工具调用兜底通道，主对话仍走 DeepSeek |

**选型理由**（按权重）：零迁移（finChat 全链路已在 DeepSeek 上调优）> function calling 可靠 > 中文强 > 便宜（日 token 熔断 120k 成本可控）。

**视觉注意（阶段 3 时）**：DeepSeek 视觉 API 会把图片自动压缩到约 800×800（384 token/图）。订阅截图是手机 UI 大字号（订阅名/金额/扣费日），够用；若未来要识别密集小字凭证，需补专业 OCR。

### 4.3 录入口径：直接录「下次扣费日期」（最小摩擦）

**设计原则：让用户照抄，不让用户算。**

用户打开微信「我→服务→钱包→支付设置→自动续费」、支付宝「免密支付/自动扣款」、或任意平台 App 的会员页，**每个订阅都直接显示「下次续费/扣费日期」**——这是用户最容易获取、零计算的信息。而「首次购买日」「首次扣费日」很多平台根本不展示，用户得翻订单记录，摩擦大、易放弃录入。

| 信息 | 用户获取难度 | 获取路径 |
|---|---|---|
| **下次扣费日** `nextCharge` | ★ 最易 | 微信/支付宝自动续费页、平台会员页，直接显示 |
| 首次购买日 | ★★★ | 得翻订单记录，很多平台不展示 |
| 首次扣费日 | ★★★★ | 财务概念，用户不关心，得自己推算 |

**结论：主录入字段直接用「下次扣费日期」`nextCharge`，`cycleDay` 由系统反推。**

| 字段 | 来源 | 说明 |
|---|---|---|
| `nextCharge` | **用户照抄 / 账本君解析** | **主录入字段**，如「2026-09-15」——用户打开平台看到的续费日 |
| `cycleDay` | 系统自动反推 | `day(nextCharge)`，如 15；yearly 取「月-日」如 `09-15` |
| `firstChargeDate` | 用户可选补充 | **可选**，仅年度报告算「已订阅几个月」用；不填系统用 nextCharge 减一个周期估算 |

**录入后维护**：每次扣费到期后，系统按 cycle 自动滚动推算下一个 `nextCharge`（用户不用管）。`firstChargeDate` 一旦填了就固定不变，作为订阅起点留存。

**与「空状态录入引导」闭环**：T1.2 引导用户去微信/支付宝自动续费页照抄——用户在那看到的正是「下次扣费日」，抄完直接填进 `nextCharge`，全程零计算。

---

## 5. 阶段 1 任务清单（叠加验证，1-2 周）

> 验收总标准：用户能新增/编辑/删除订阅，订阅到期前能收到订阅消息提醒，能通过账本君对话「AI 智能录入」订阅。

### T1.1 数据层：subscriptions 集合 + db.js CRUD + dbRead

**改动文件**：

1. `utils/db.js` —— 新增 5 个方法（照抄 `addRecurring/listRecurring/updateRecurring/removeRecurring` 模式）：

   - `addSubscription(data)`：`db.collection('subscriptions').add({ data: { ...data, status:'active', deleted:false, createdAt: db.serverDate(), updatedAt: db.serverDate() } })`，`invalidate()`；集合未创建时抛 `isCollectionMissing`（照抄 addRecurring 的 -502005 兜底）。入库前由 `nextCharge` 反推 `cycleDay = day(nextCharge)`（标准周期；custom 无 cycleDay）
   - `listSubscriptions(force)`：走 `cloudRead('listSubscriptions')`，`cache.subscriptions` TTL 缓存（照抄 listRecurring）
   - `updateSubscription(id, data)`：update + `invalidate()`
   - `removeSubscription(id)`：软删 `{ deleted:true, deletedAt: db.serverDate() }` + `invalidate()`
   - 计算辅助 `nextChargeOf(cycle, currentNextCharge, now, customMonths)`：从当前 nextCharge 出发按周期滚动到「now 之后最近一次」——**仅在订阅到期后滚动更新用**（如 remind 触发器扣减后 / 用户主动「标记已续费」按钮）；用户首次录入不走这条路径（nextCharge 是用户直接抄进的主字段，不是推算的）。标准周期用 `cycleDay = day(currentNextCharge)` 滚动，`cycle=custom` 时按 `customMonths` 整月累加。月末溢出用 `dayInMonth` clamp（前端 `utils/util.js` 实现一份，云函数端独立一份）

2. `cloudfunctions/dbRead/index.js` —— 新增 `listSubscriptions` 读取：

   - 在 `RECYCLE_COLS` 数组加入 `'subscriptions'`
   - 新增 `async function listSubscriptions(openid)`（照抄 `listRecurring`）：`where({ _openid: openid, deleted: _.neq(true) }).limit(MAX).get()`，`sort(cmpAsc)`
   - 在 `exports.main` 的 switch 加 `case 'listSubscriptions': data = await listSubscriptions(OPENID).catch(missingAsEmpty)`
   - **并入 batchHomeRead**：`batchHomeRead` 的 `Promise.all` 加 `listSubscriptions(openid)`，返回对象加 `subscriptions` 字段 —— 首页「待办账务」区块（T1.5）依赖它，否则首页拿不到订阅数据

3. 云开发控制台：新建集合 `subscriptions`，权限「仅创建者可读写」

**验收**：`db.js` 5 个方法可用；`dbRead` 返回 `listSubscriptions` 空数组不报错；`subscriptions` 进回收站扫描

**验证脚本**：`scripts/verify-subscriptions-data.js`（断言：db.js 含 5 个方法名；dbRead 的 RECYCLE_COLS 含 subscriptions；switch 含 listSubscriptions 分支）

---

### T1.2 订阅页 UI

**改动文件**：

1. `app.json` —— 注册新页面 `pages/subscriptions/subscriptions`
2. `pages/subscriptions/subscriptions.{js,wxml,wxss,json}` —— 新建，**参考 `pages/my/my.js` 的固定支出弹框实现**：
   - 列表展示：订阅名 + 平台 + 金额 + 周期 + 下次扣费日 + 使用频率标签 + 状态
   - 新增/编辑：底部弹框表单（`.sheet` 那套 flex 布局，`scroll-view.sheet-scroll` + `catchtouchmove="preventTouchmove"`，照抄 my 页 recur 弹框的结构与 `recurScrollHeight` 自适应逻辑）。表单字段（录入口径见 4.3 节）：
     - **下次扣费日期 / 首次到期日期**（主字段）：`<picker mode="date">` 日期选择器——用户**打开订阅 App 会员中心照抄「有效期至」即可**，零计算（详见 4.3 节）。录入后系统自动反推 `cycleDay = day(nextCharge)`，并用 `nextCharge - 1 周期` 估算 `firstChargeDate`（年度报告用）
     - 下方只读展示推导结果：标准周期显示「每月扣费日 X 号」+「已订阅约 N 个月」；`custom` 周期显示「每 N 个月续费一次」+「已订阅约 N 个月」
     - **快捷选项**：①「今天新开」→ `nextCharge = 今天 + 1 周期`（用户今天刚订阅，等价于下一期到期日）。**取消「不记得了」降级**——到期日是 App 里直接看得到的，理论上不存在记不住的情况；如果用户真不记得，可以让用户先去看一眼 App 再回来填（比「猜一个日期」更准）
     - **录入引导（冷启动友好）**：字段下方放小字「不知道填哪天？打开「腾讯视频/爱奇艺」App → 我 → 会员中心 → 会员有效期，照着填」
     - **扣费周期**：picker 选「每月 / 每季 / 每年 / 每周 / 自定义」。选「自定义」时展开月数输入框（`<input type="number">`，占位「如 6 = 半年包，3 = 季包」），存入 `customMonths`
     - **扣费渠道**：单选 picker（微信自动续费 / 支付宝自动扣款 / 苹果订阅 / App 内开通 / 不清楚），默认「不清楚」——决定取消指引主路径（见 T2.3 两级匹配）
   - **空状态录入引导（冷启动关键）**：订阅列表为空时展示提示——「不知道自己开通了哪些自动续费？打开 **微信：我 → 服务 → 钱包 → 支付设置 → 自动续费**，或 **支付宝：我的 → 设置 → 支付设置 → 免密支付/自动扣款**，把你已开通的项目照着抄进来」。这两个页面就是用户在微信/支付宝侧的真实订阅清单，把「想不起来」变成「照着抄」，也顺带让用户自然感知 `payChannel` 该选哪个
   - 删除：软删 + 二次确认
   - 顶部汇总卡：月均订阅支出 / 年化订阅总支出（`monthly×12 + yearly×1 + quarterly×4 + weekly×52 + custom→amount×(12/customMonths)` 求和）
3. `pages/my/my.wxml` + `my.js` —— 「我的」页资产区块加**次入口** cell「订阅续费管理」（与「信用卡管理」并列），`navigateTo` 到 `pages/subscriptions/subscriptions`，满足习惯去设置里找功能的用户

**入口策略（主 + 次双入口）**：
- **主入口**：首页「今天要处理」升级为「待办账务」区块，订阅续费与信用卡还款两类待办合并展示，点订阅项跳订阅页（见 T1.5）
- **次入口**：「我的」页资产区块的「订阅续费管理」cell（本任务第 3 点）
- **不新增 Tab**（tab 已 4 个）。验证需求后阶段 3 再决定是否升 Tab

**验收**：能增删改查订阅；录「下次扣费日期 / 首次到期日期」后系统自动反推扣费日与首次扣费日；列表按下次扣费日排序；汇总卡数字正确；软删后进回收站可恢复

**验证脚本**：`scripts/verify-subscriptions-page.js`（断言：app.json 注册页；subscriptions.js 有 loadSubscriptions/saveSubscription/removeSubscription；wxml 有 `.sheet` 结构 + `mode="date"` 日期选择器 + **nextCharge 主字段** + payChannel 选项 + 自定义周期选项 + customMonths 输入 + 到期日录入引导文案 + **无 firstChargeDate 录入字段**；my.wxml 有入口 cell）

---

### T1.3 到期提醒（remind 云函数加订阅分支）

**改动文件**：

1. `cloudfunctions/remind/index.js` —— 在现有还款扫描之后，加订阅到期扫描：

   ```js
   // 订阅到期提醒：nextCharge = 明天 或 今天的 active 订阅
   const subsRes = await db.collection('subscriptions')
     .where({ status: 'active', deleted: _.neq(true) }).limit(1000).get()
   const dueSubs = subsRes.data.filter((s) => s.nextCharge === tomorrowStr || s.nextCharge === todayStr)
   // 按 _openid 聚合 → cloud.openapi.subscribeMessage.send（新模板 ID）
   ```

   - 复用现有 `nowInChina()` / `fmtDate()` / `dayInMonth()`（`lib/date.js` 已有）
   - 到期提醒分两档：`nextCharge === tomorrowStr`（明天到期）推提醒；`nextCharge === todayStr`（今天到期）推提醒（可合并为「明天或今天到期」）

2. `utils/config.js` —— 新增 `SUBSCRIPTION_REMIND_TEMPLATE_ID`（订阅到期提醒模板 ID）

3. `cloudfunctions/remind/index.js` 顶部 —— 新增对应 `SUB_TEMPLATE_ID` 常量（与 config 同步）

4. **订阅消息模板申请**（人工，去 mp 后台）：
   - `mp.weixin.qq.com` → 功能 → 订阅消息 → 选用「到期提醒 / 续费提醒」类**一次性订阅**模板
   - 建议关键词：`thing1`（订阅名称）、`date2`（扣费日期）、`amount3`（金额）、`thing4`（提醒语）

**验收**：active 订阅 `nextCharge` 为明天时，次日 9:00 触发器能扫到并推送（需真机 + 已授权）

**验证脚本**：`scripts/verify-subscriptions-remind.js`（断言：remind/index.js 含 subscriptions 扫描；含 SUB_TEMPLATE_ID；日期用 nowInChina/fmtDate 而非裸 new Date）

---

### T1.4 AI 智能录入（finChat 加 addSubscription 工具）

**改动文件**：`cloudfunctions/finChat/index.js`

1. 在 `TOOL_DEFS` 数组末尾新增 `addSubscription` 工具 schema：

   ```js
   {
     type: 'function',
     function: {
       name: 'addSubscription',
       description: '当用户想记录一笔订阅/自动续费时调用。例:"记个订阅 爱奇艺每月25""百度网盘年费298""网易云音乐包月15""腾讯视频半年包88""爱奇艺到9月15号每年298""微信扣的我这个爱奇艺每月25"。识别订阅名称、金额、周期、下次扣费日期(主字段 nextCharge,用户说"到期X号/有效期至X/会员到X"就解析这个——用户对着 App 会员中心直接抄,零计算);只说每月几号扣的降级 cycleDay。金额/周期不全可用合理默认并如实告知,但 **nextCharge 缺失时禁止调用本工具**——先反问用户"会员到哪天到期?打开App会员中心看一眼有效期至告诉我",凑齐再调。用户说"半年包/季包/N个月包"这类非标准周期时用 cycle=custom + customMonths。只在用户明确表达"记录订阅"意图时调用;讨论/分析不调用。',
       parameters: {
         type: 'object',
         properties: {
           name: { type: 'string', description: '订阅名称,如"爱奇艺"' },
           amount: { type: 'number', description: '单期金额(元),正数,≤2位小数' },
           cycle: { type: 'string', enum: ['monthly','yearly','quarterly','weekly','custom'], description: '扣费周期,默认 monthly。用户说"半年包/季包/N个月包/两年包"等非标准周期时用 custom,并填 customMonths' },
           customMonths: { type: 'number', description: '仅 cycle=custom 时有效:一个周期含多少个月。半年包=6,季包=3,两年包=24。标准周期不传' },
           nextCharge: { type: 'string', description: '**下次扣费日期 / 首次到期日期 YYYY-MM-DD,主录入字段**。如"2026-09-15"。用户说"到期X号/有效期至X/会员到X/9月15号扣"就解析这个;用户对着 App 会员中心「会员有效期至」照抄,零计算。系统自动反推 cycleDay=day(nextCharge) 和 firstChargeDate=nextCharge-1 周期' },
           payChannel: { type: 'string', enum: ['wechat','alipay','apple','inapp','unknown'], description: '扣费渠道:用户说"微信自动续费/微信扣的"=wechat,"支付宝自动扣款"=alipay,"苹果订阅/App Store订的"=apple,"App里开的"=inapp,默认 unknown' },
           cycleDay: { type: 'string', description: '降级兜底:用户只记得每月几号扣费时用,monthly/quarterly/weekly 存 1-31 数字字符串;yearly 存 "MM-DD"。传了 nextCharge 就不用传这个' },
           usage: { type: 'string', enum: ['frequent','occasional','rare','never'], description: '使用频率自评,默认 rare' },
           confirmed: { type: 'boolean', description: '查重冲突后用户明确确认"要新增重复订阅"时传 true 跳过查重(如家里确实有两条爱奇艺)。默认 false' }
         },
         required: ['name', 'amount', 'nextCharge']
       }
     }
   }
   ```

2. 新增执行器 `executeAddSubscription(args, openid)`（照抄 `executeAddExpense` 的安全校验风格）：
   - 校验：`amount` 合法；`cycle` 白名单；`nextCharge` 合法（YYYY-MM-DD 格式，日期在合理范围内）；`name` 非空且 ≤ 20 字；`cycleDay` 仅作降级兜底（1-31 或 MM-DD）；`payChannel` 白名单（缺省 `unknown`）；`cycle=custom` 时 `customMonths` 必须为 1-36 的正整数（防荒谬值），且必须有 `nextCharge`（custom 必须有到期日）
   - **口径归一**：传了 `nextCharge` → `cycleDay = day(nextCharge)`（yearly 取 MM-DD），`firstChargeDate = nextCharge - 1 周期`（年度报告用，可选字段，不传也不报错）；只传 `cycleDay` → `nextCharge` 用「本月该日」反填（本月该日未过则本月，已过则下月）
   - **写入前查重（防重复录入的闸门）**：add 前先查库——`where({ _openid, deleted: _.neq(true) }).get()` 后按 name 匹配（精确相等 或 双向包含，如「腾讯视频」vs「腾讯视频VIP」），命中 `status !== 'cancelled'` 的同名订阅且 `confirmed !== true` 时**不写入**，返回 `{ ok: false, conflict: true, existing: { name, amount, cycle, nextCharge } }`，让 AI 转述给用户：「你已有一条腾讯视频订阅（¥25/月，下次扣费 09-15），是要改这条还是再记一条？」；用户答「再记一条」→ AI 带 `confirmed: true` 重调；答「改」→ 引导去订阅页或说清楚改什么。软删记录（deleted: true）**不参与查重**——用户删了再录是正常操作，必须放行
   - 写入 `subscriptions` 集合（**必须显式写 `_openid`**，云函数端 add 不自动注入）
   - `nextCharge` 已是用户录入的最终值，**录入时无需调用 nextChargeOf 推算**（nextChargeOf 仅在订阅到期后滚动下一期时用）
   - 返回 `{ ok: true, id, sub }` 或 `{ ok: false, reason }`

3. 在 `callLLM` 工具分发处注册：
   - `RECORD_TOOLS` 数组加 `'addSubscription'`
   - 工具分发 switch 加 `addSubscription` 分支（返回确定性确认语，不再额外调 LLM，沿用「单轮至多 1 次工具调用」纪律）。确认语模板按字段完整度分两种：
     - 字段全：`✓ 已记订阅 爱奇艺 ¥25/月（微信自动续费），下次扣费 09-15`
     - payChannel 为 unknown：`✓ 已记订阅 腾讯视频 ¥88/半年，下次到期 2027-03-15。对了，是在微信/支付宝/苹果里开通的吗？告诉我，取消订阅时我给你精确路径`（**非阻塞顺带问**——用户答了下轮更新，不答也不影响）

4. 在 `PROMPT_RECORD` 段追加 `addSubscription` 使用规则：
   - 触发词：记订阅/订阅/续费/包月/年费/会员
   - **字段缺失分层追问纪律（核心，防静默脏数据）**：

     | 字段 | 缺失时策略 | 理由 |
     |---|---|---|
     | `nextCharge`（到期日） | **阻塞式追问**：不调工具，先问「会员到哪天到期？打开 App 会员中心看一眼『有效期至』告诉我」；用户回答后带上下文（会话历史已支持）再调工具 | 提醒功能的命根子；默认「今天+1周期」对老订阅必错 |
     | `platform` | **不追问**，用 `name` 直接填 | name≈platform 占绝大多数（腾讯视频→腾讯视频） |
     | `usage` | **不追问**，默认 `rare`，确认语注明「使用频率按"很少"记了，可随时改」 | 阶段 2 评估时再引导确认更合适 |
     | `payChannel` | **非阻塞顺带问**：照常录入（unknown），确认语末尾带一句渠道问题 | 只影响取消指引精度（双路径兜底），不值得为此多一轮阻塞 |

   - **追问纪律**：一次最多 1 个阻塞式问题（只允许 nextCharge），**禁止连环问**（不许一口气问「哪天到期？什么渠道？用得多吗？」）；用户明确说「不知道/不想说」时按默认值录入并如实告知，不再纠缠
   - **数据库优先纪律（防历史幻觉，核心）**：「是否已录入过」**只能以工具执行结果/数据库现状为准，禁止以会话历史为准**——用户可能已在订阅页删除/修改过，历史里聊过"已录入"不代表现在还存在。典型场景：用户上轮录入腾讯视频 → 在订阅页删了 → 这轮说「记个腾讯视频」→ AI **不得说"之前已经录入过了"**，直接调工具查重（软删记录不参与查重，正常放行新增）
   - **conflict 处理纪律**：工具返回 `conflict: true` 时，**禁止编造"已记上"**（库里实际没写入）——必须原样转述冲突信息让用户二选一（改这条 / 再记一条），用户确认后才能带 `confirmed: true` 重调

**验收**：账本君对话输入「记个订阅 爱奇艺每月25」→ 工具被调用，subscriptions 集合多一条记录，回复带确认语；输入「爱奇艺到9月15号每年298」→ `nextCharge=2026-09-15`、`cycle=yearly` 正确入库，系统反推 `cycleDay=15`、`firstChargeDate=2025-09-15`；输入「我微信开了个爱奇艺自动续费每月25」→ `payChannel=wechat` 正确入库；输入「腾讯视频半年包88 到期2026年9月4号」→ `nextCharge=2026-09-04`、`cycle=custom`、`customMonths=6`、年化 176 正确入库；**输入「腾讯视频半年包88」（未说到期日）→ AI 不调用工具，反问到期日；用户补答「9月4号」后 → 带上下文正确入库（多轮 slot filling）**；**库里已有 active「腾讯视频」时再说「记个腾讯视频」→ 工具返回 conflict 不写入，AI 转述冲突让用户选；用户说「再记一条」→ 带 confirmed:true 重调成功**；**用户删掉腾讯视频后再说「记个腾讯视频」→ 软删记录不拦，正常新增，且 AI 不说「之前已经录入过了」**

**验证脚本**：`scripts/verify-subscription-tool.js`（断言：TOOL_DEFS 含 addSubscription 且 schema 含 **nextCharge 主字段** + customMonths + cycle enum 含 custom + **required 含 nextCharge** + **confirmed 参数**；description 含「禁止调用本工具」追问规则；RECORD_TOOLS 含 'addSubscription'；分发处有 addSubscription 分支 + payChannel unknown 顺带问确认语 + **conflict 转述确认语**；executeAddSubscription 含 _openid 显式写入 + **nextCharge→cycleDay/firstChargeDate 反推** + customMonths 校验 + **写入前查重（name 匹配 + conflict 返回 + confirmed 放行 + 软删不参与）**；PROMPT_RECORD 含分层追问纪律 + **数据库优先纪律 + conflict 处理纪律**）

---

### T1.5 首页「待办账务」区块改造（主入口）

**改动文件**：`pages/index/index.js` + `index.wxml` + `index.wxss`

1. `pages/index/index.js` —— `todoList` 合并订阅待办（在现有还款循环 `cards.forEach`，约 200-231 行之后追加）：
   - 每条待办加 `type: 'card' | 'sub'` 字段
   - 遍历 `batch.subscriptions`（T1.1 已并入 batchHomeRead），取 `status === 'active'` 且 `nextCharge` 落在「已过扣费日未取消 / 今天 / 明天」范围内的订阅，push 进 `todoList`
   - 订阅项字段：`{ id, type:'sub', name, amount, days, dueText, level, canPay:false }`
   - 统一按 `days` 升序排序（日期越近越紧急，两类混排）；订阅只取最近 1-2 条进区块，其余进二级页，避免刷屏
   - **订阅 level 语义与还款相反（重点，别套用还款三档）**：
     - `days === 1` → level `tomorrow`，dueText `明天扣费`（custom 周期 + payChannel 非 wechat/alipay/apple 时改为 `明天到期`——半年包等期限包到期不一定自动扣）
     - `days === 0` → level `today`，dueText `今天扣费`（同上，期限包改 `今天到期`）
     - `days < 0` → level `overdue`，dueText `已扣费·未取消`（不是「逾期」，是提醒"上期白扣了 X，要不要断舍离"；期限包改 `已过期·未续费`）

2. `pages/index/index.wxml` —— 区块升级：
   - 标题「今天要处理」→「待办账务」
   - `todo-item` 按 `type` 分支渲染：`card` 保留 `{{item.bank}}` + 「标记已还」按钮；`sub` 显示 `{{item.name}}` + 金额 + 扣费日，**无「标记已还」按钮**，整条 `bindtap` 跳 `pages/subscriptions/subscriptions`
   - 区块右下角加「订阅管理」小字入口（`navigateTo` 订阅页）
   - 无待办（两类都空）时整卡隐藏（现有 `wx:if="{{todoList.length}}"` 行为不变）

3. `pages/index/index.wxss` —— `.todo-item` 复用现有样式，按需补订阅项展示样式

**验收**：有 active 订阅且 nextCharge 明天/今天/已过时，首页「待办账务」出现订阅项（订阅名 + 金额 + 扣费日）；点订阅项跳订阅页；还款项「标记已还」不受影响

**验证脚本**：`scripts/verify-home-todo-merge.js`（断言：index.js todoList 含 type 字段 + 订阅循环；wxml 标题含「待办账务」；wxml 有 type 分支 + 「订阅管理」入口；订阅 level 三档语义文案正确）

---

### 阶段 1 交付物清单

| 任务 | 改动文件 | 新增集合 | 验证脚本 |
|---|---|---|---|
| T1.1 数据层 | `utils/db.js`、`cloudfunctions/dbRead/index.js` | `subscriptions` | verify-subscriptions-data.js |
| T1.2 UI | `app.json`、`pages/subscriptions/*`、`pages/my/*` | — | verify-subscriptions-page.js |
| T1.3 提醒 | `cloudfunctions/remind/index.js`、`utils/config.js` | — | verify-subscriptions-remind.js |
| T1.4 AI 录入 | `cloudfunctions/finChat/index.js` | — | verify-subscription-tool.js |
| T1.5 首页待办 | `pages/index/index.js`、`index.wxml`、`index.wxss` | — | verify-home-todo-merge.js |

---

## 6. 阶段 2 任务清单（AI 断舍离核心，2-3 周）

> 验收总标准：订阅可做 AI 价值评估（去留建议 + 免费平替 + 冲动判断）；有年度浪费报告；到期/评估时附平台取消指引。

### T2.1 价值评估（finChat 加 evaluate_subscription 工具）

**改动文件**：`cloudfunctions/finChat/index.js`

1. `TOOL_DEFS` 新增 `evaluate_subscription` 工具：

   ```js
   {
     type: 'function',
     function: {
       name: 'evaluate_subscription',
       description: '对用户的一个订阅做「断舍离」价值评估。读取该订阅的金额/周期/使用频率(usage),结合订阅名称/平台判断:使用频率是否配得上价格、有无免费平替、是否冲动订阅,给出去留建议与省钱方案。当用户问"这个订阅要不要留""爱奇艺还值不值"时调用。',
       parameters: {
         type: 'object',
         properties: {
           name: { type: 'string', description: '订阅名称或平台,用于定位用户已记录的订阅' }
         },
         required: ['name']
       }
     }
   }
   ```

2. 新增执行器 `executeEvaluateSubscription(args, openid)`：
   - 查 `subscriptions` 集合，按 `name` 模糊匹配（`where({ name: db.RegExp(...) })` 或全量拉取后 JS 匹配），找不到则返回 `{ ok:false, reason:'还没记录这个订阅' }`
   - 把订阅字段（name/platform/payChannel/amount/cycle/usage/年化金额）拼成数据块，作为工具结果返回给 LLM 生成评估结论
   - **关键：usage 是评估核心，但只能靠用户自评**。若 `usage` 为空/rare，评估里要**引导用户确认使用频率**，不要 AI 自己编

3. 工具分发处加 `evaluate_subscription` 分支：工具查库后，把结果交给第 2 次 LLM 生成评估语（这是「查询工具」类，参考 `handleQueryTool` 模式：查库 → 拼数据 → 决定用确定性模板 or 追加润色）

4. `PROMPT_CHAT` / `PROMPT_RECORD` 补评估规则：
   - 评估结构：先给结论（留/砍/观望）→ 依据（年化金额 + 使用频率）→ 免费平替 → 省钱金额
   - 硬约束：数字只能来自订阅字段；免费平替判断要基于常识但**不得编造价格**（不确定就说「有免费平替，具体可自行查」）

**验收**：账本君问「爱奇艺还值不值」→ 返回结构化评估（含年化金额、频率判断、平替建议）

**验证脚本**：`scripts/verify-subscription-eval.js`（断言：TOOL_DEFS 含 evaluate_subscription；分发分支存在；executeEvaluateSubscription 含 usage 引导逻辑）

---

### T2.2 年度浪费报告（新增 subReport 云函数）

**改动文件**：

1. `cloudfunctions/subReport/index.js` —— **新建**，仿写 `finReport`：

   - 环境变量复用 `LLM_API_KEY / LLM_BASE_URL / LLM_MODEL`
   - 聚合逻辑：拉取用户全部 active/paused 订阅 → 算年化金额（`monthly×12 + yearly×1 + quarterly×4 + weekly×52 + custom→amount×(12/customMonths)`）
   - 「浪费」口径：`usage === 'never'` 的订阅年化金额全计入浪费；`usage === 'rare'` 计入 50%（系数可调）；`occasional` 计入 0；`frequent` 不计入
   - 数据块拼法照抄 `finReport.formatDataForLLM`：逐条列订阅（名称/年化金额/频率/浪费判定）+ 年总支出 + 年浪费总额 + 优化后金额
   - SYSTEM_PROMPT 仿写 finReport：语气「账本君」，先给结论（一年订阅花了 X、其中疑似浪费 Y）→ 逐条给去留建议 → 收尾给一个可执行省钱数字
   - 缓存：仿 finReports，新增 `subReports` 集合（`_openid + year + ver`），CACHE_VER=1
   - **cost 控制**：单次 max_tokens 300、temperature 0.5，与 finReport 一致

2. `cloudfunctions/subReport/package.json` + `config.json` —— 仿 finReport（无触发器，纯调用）

3. `utils/db.js` —— 加 `getSubReport(year)`（调 subReport 云函数，带 60s TTL 缓存）+ `invalidateSubReport(year)`

4. 前端入口：`pages/subscriptions/` 页顶部加「年度浪费报告」按钮 → 调 `db.getSubReport` 展示

5. 云开发控制台：新建集合 `subReports`，权限「仅创建者可读写」

**验收**：有订阅数据的用户点击报告 → 得到一段 AI 年度浪费报告（含总额/浪费额/逐条建议）

**验证脚本**：`scripts/verify-sub-report.js`（断言：subReport 目录存在 index.js/package.json/config.json；含年化聚合 + 浪费口径计算；db.js 含 getSubReport/invalidateSubReport；含 subReports 缓存 ver）

---

### T2.3 取消指引（结构化内容库 + AI 匹配）

**改动文件**：

1. `cloudfunctions/subReport/cancelGuides.js`（或 `cloudfunctions/finChat/cancelGuides.js`）—— **新建结构化内容库，两级匹配（渠道 × 平台）**：

   > **为什么两级**：同一个爱奇艺，通过微信自动续费开通的，取消要去微信支付设置关，去爱奇艺 App 里关不掉。**「在哪开的」（payChannel）比「开的什么」（platform）更决定取消路径**——渠道级路径通用且稳定，平台级只兜「App 内开通」场景。

   ```js
   // 两级结构：渠道决定取消主路径（覆盖绝大多数场景），平台级仅补「App 内开通」。
   // 步骤是「内容资产」，人工维护 + 定期校验，禁止 AI 现场编造。
   const CHANNEL_GUIDES = {
     wechat: '微信 → 我 → 服务 → 钱包 → 支付设置 → 自动续费 → 选择项目 → 关闭',
     alipay: '支付宝 → 我的 → 设置（右上角齿轮）→ 支付设置 → 免密支付/自动扣款 → 选择项目 → 关闭',
     apple: 'iOS 设置 → 顶部姓名（Apple ID）→ 订阅 → 选择项目 → 取消订阅',
   }
   const PLATFORM_GUIDES = {
     // 仅 payChannel = inapp / unknown 时匹配
     '爱奇艺': '爱奇艺App → 我的 → 会员 → 自动续费管理 → 关闭自动续费',
     '腾讯视频': '...',
     '优酷': ...,
     '芒果TV': ...,
     '网易云音乐': ...,
     'QQ音乐': ...,
     '百度网盘': ...,
     'iCloud/Apple': ...,
     '美团': ...,
     '饿了么': ...,
   }
   module.exports = { CHANNEL_GUIDES, PLATFORM_GUIDES }
   // 匹配优先级：
   // 1. payChannel ∈ {wechat,alipay,apple} → 直接给 CHANNEL_GUIDES[payChannel]
   // 2. payChannel ∈ {inapp,unknown} → 按 platform 匹配 PLATFORM_GUIDES
   // 3. 双兜底：渠道 unknown + 平台未命中 → 同时给微信 + 支付宝两条通用路径，让用户自己对照
   ```

2. `finChat/index.js` —— 在 `evaluate_subscription` 的返回里，**按上述两级优先级命中内容库则附取消步骤**；`addSubscription` 录入后也提示「到期提醒会附取消入口」

3. 前端 `pages/subscriptions/` —— 订阅详情/卡片展示「取消指引」入口（点击展开步骤，按匹配结果展示对应渠道路径；双兜底场景微信/支付宝两栏并列）

**验收**：录入爱奇艺（payChannel=wechat）后，取消指引给的是微信支付设置路径而非爱奇艺 App 路径；payChannel=inapp 时给 App 内路径；渠道与平台都未命中时给微信/支付宝双路径

**验证脚本**：`scripts/verify-cancel-guides.js`（断言：cancelGuides.js 存在且 CHANNEL_GUIDES 含 wechat/alipay/apple 三键 + PLATFORM_GUIDES 含至少 8 个平台；finChat 评估返回含两级匹配逻辑；前端有取消指引展示）

---

### T2.4 订阅数据纳入账本君数据块

**改动文件**：

1. `utils/aiChat.js` —— `serialize()` 加订阅摘要字段：
   - `subscriptions`: 订阅摘要数组（name/platform/payChannel/amount/cycle/usage/nextCharge，最多 10 条）
   - `subYearlyTotal`: 年化订阅总支出
   - 首页/订阅页把订阅数据传给 `send({ stmt })` 时带上

2. `cloudfunctions/finChat/index.js` —— `buildMessages` / `formatDataForLLM` 数据块加订阅段，让账本君能回答「我一年订阅花多少」

3. 首页数据链路：T1.5 已把 subscriptions 并入 batchHomeRead，首页 `loadData` 可直接把订阅摘要透传给账本君（不再是「可选」）

**验收**：在订阅页问账本君「我订阅一年多少钱」→ 能答出年化总额

**验证脚本**：`scripts/verify-sub-serialize.js`（断言：aiChat.serialize 含 subscriptions/subYearlyTotal；finChat 数据块含订阅段）

---

### 阶段 2 交付物清单

| 任务 | 改动文件 | 新增集合 | 验证脚本 |
|---|---|---|---|
| T2.1 价值评估 | `cloudfunctions/finChat/index.js` | — | verify-subscription-eval.js |
| T2.2 年度报告 | `cloudfunctions/subReport/*`、`utils/db.js`、`pages/subscriptions/*` | `subReports` | verify-sub-report.js |
| T2.3 取消指引 | `cloudfunctions/*/cancelGuides.js`、`finChat`、`pages/subscriptions/*` | — | verify-cancel-guides.js |
| T2.4 数据块 | `utils/aiChat.js`、`cloudfunctions/finChat/index.js` | — | verify-sub-serialize.js |

---

## 7. 验证脚本怎么跑

项目已有 `scripts/verify-*.js` 的静态断言模式，新脚本照此风格写（纯 Node，无云环境依赖，校验代码结构/字段/class/方法名是否到位）：

```bash
# 用系统 node（脚本只做字符串/正则断言，不需要云环境）
node scripts/verify-subscriptions-data.js
node scripts/verify-subscriptions-page.js
node scripts/verify-subscriptions-remind.js
node scripts/verify-subscription-tool.js
node scripts/verify-home-todo-merge.js
node scripts/verify-subscription-eval.js
node scripts/verify-sub-report.js
node scripts/verify-cancel-guides.js
node scripts/verify-sub-serialize.js

# 全部通过后，跑一遍存量脚本回归，确保没破坏现有功能
node scripts/verify-scroll-lock.js
node scripts/verify-recur-link.js
```

**验证脚本断言原则**（照抄现有 verify 脚本风格）：

- 每个断言 `assert(条件, '说明')`，全绿输出「X 项断言全过」
- 断言对象是**静态代码特征**：方法名存在、switch 分支存在、集合名在 RECYCLE_COLS、prompt 关键词存在、字段名拼写正确
- 不依赖真机、不依赖云环境 —— 真机订阅消息推送单独手工验证

---

## 8. 部署清单（阶段 1 + 2 完成后统一执行）

按顺序执行，每步都可验证：

1. **云开发控制台**：新建集合 `subscriptions`、`subReports`，权限「仅创建者可读写」
2. **云函数重新部署**（右键 → 上传并部署：云端安装依赖）：
   - `dbRead`（T1.1，含 listSubscriptions + batchHomeRead 并入 subscriptions）
   - `finChat`（T1.4 / T2.1 / T2.3 / T2.4）
   - `remind`（T1.3）
   - `subReport`（T2.2，新建，环境变量配 LLM_API_KEY / LLM_BASE_URL / LLM_MODEL，与 finReport 一致）
3. **订阅消息模板**：mp 后台申请「订阅到期提醒」一次性模板，填入 `utils/config.js` 的 `SUBSCRIPTION_REMIND_TEMPLATE_ID` + `remind/index.js` 的 `SUB_TEMPLATE_ID`
4. **前端重编译**：上传体验版，真机验证订阅消息（开发者工具不支持订阅消息推送）
5. **回归**：跑第 7 节全部脚本 + 存量脚本

---

## 9. 风险与备注

| 风险 | 应对 |
|---|---|
| 订阅消息「一次性」导致到期提醒无法持续推 | 在「记录订阅 / 查看订阅列表 / 完成 AI 评估」等高意愿节点回收授权（用户每操作一次授权一次） |
| 「使用频率」AI 无法自动获知 | 只用用户自评 usage 三档，评估结论明确标注「基于你的自评」 |
| 取消指引内容过时/编错 | 结构化内容库人工维护；AI 只匹配不编造；库未命中的给「微信/支付宝自动扣费」通用入口 |
| AI 评估编造免费平替价格 | prompt 硬约束：不确定就明说「有平替，具体价格自行查」，禁止编数字 |
| AI 追问导致录入变繁琐 | 分层追问：只对 `nextCharge` 阻塞式追问（提醒功能命根子，最多多一轮）；platform 用 name 兜底不问、usage 默认 rare 不问、payChannel 确认语顺带问（非阻塞）。用户说「不知道」立即按默认录入，不再纠缠 |
| AI 以会话历史为据说"已录入过"（实际已被用户删除）→ 幻觉 + 重复录入 | 执行器写入前查重闸门（name 匹配 + conflict 返回 + confirmed 放行）；PROMPT_RECORD「数据库优先」纪律：已录与否只看工具执行结果，不看会话历史；软删记录不参与查重（删了再录正常放行） |
| 云函数冷启动 + LLM 延迟 | 复用现有「单轮至多 1 次工具调用」纪律 + finTemplate 兜底 + 60s TTL 缓存 |
| expAgg 双写同步（P0-1 历史教训） | 订阅**不涉及** expAgg（订阅是独立维度，不是支出流水）；若未来「订阅自动落账到支出」才需要补 expAgg 维护，当前不做 |

---

## 10. 下一步

本文档覆盖阶段 1/2 全部实现细节。实现时**建议顺序**：T1.1 → T1.2 → T1.5 → T1.4 → T1.3 →（阶段 1 验收）→ T2.1 → T2.3 → T2.2 → T2.4 →（阶段 2 验收）→ 部署清单。

阶段 3（个人主体虚拟支付接会员、激励视频、流量主、独立决策）需在阶段 1/2 跑通、拿到真实使用数据后再启动，届时另出文档。
